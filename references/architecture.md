# Arquitetura — Calculadora de Pão com Fermento Natural (v5)

> Documento vivo, mantido pelo agente `escriba` a cada iteração do loop.
> Especificação do produto: `specs/` — `Calculadora_Pao_Fermento_Natural_v5.md` é a origem histórica, não a única canônica; doc mais novo com cabeçalho `Supera:` prevalece sobre o que ele cobre.
> Este arquivo registra **como** construímos, não **o quê**.

## Regras de ouro (do cliente, 2026-07-05 — valem para TODO o código)

1. **Prefira libs consolidadas.** Funcionalidade não-trivial (XLSX, gráficos, datas, etc.): use lib estabelecida e mantida em vez de implementar à mão. Justifique a escolha em uma linha na issue. Exceção: o core de cálculo da spec é o produto — esse é nosso, com TDD.
2. **Reuse tudo o que existir.** Antes de criar função, componente ou classe CSS: procure no código (`grep`) e no design system. Se existe, reuse; se quase existe, estenda; nunca duplique. Roda já inventada não se reinventa.
3. **Segurança e privacidade são mandatórios.** Nenhum secret em código/commit (spec §11.1); nenhuma chamada de rede em runtime do app; dados do usuário ficam 100% locais (localStorage + arquivo de backup), sem telemetria; todo dado digitado pelo usuário (nomes, observações) é renderizado com escape — nunca `innerHTML` com string bruta, nunca `eval`; `package-lock.json` commitado.
4. **Documentação validada antes de implementar.** Ao usar lib, API do navegador ou configuração não-trivial, consulte a documentação oficial na internet (docs da lib, MDN, repositório oficial) — não implemente de memória. Registre na issue o link consultado. (Vale para os agentes durante o desenvolvimento; o app em runtime continua 100% offline.)

## Stack (decisão 2026-07-04, atualizado issue 032)

- **Vite + TypeScript (strict) + Vitest**, sem framework de UI (vanilla TS + DOM).
  Motivo: app 100% client-side (spec §10); os mockups já são HTML+CSS puros sobre o design system — vanilla preserva fidelidade 1:1; as estruturas de dados da spec (§6) já são interfaces TypeScript.
- **3 páginas** (MPA do Vite): `index.html` (Receitas — rota inicial, issue 032), `receitas.html` (Calculadora), `historico.html` (Histórico) — espelham `mockups/`. **Issue 032 inversão**: GitHub Pages serve `index.html` como default da raiz `/minhapadaria/`; nenhuma forma de contornar sem trocar conteúdo. Decisão: **swap literal `index.html` ↔ `receitas.html`** — nomes preservados, conteúdo invertido. Vite rollupOptions.input inalterado. URLs antigas (`index.html?recipe=<id>`) quebram → novo destino `receitas.html?recipe=<id>`. Comentários HTML explícitos em ambos mitiga confusão. Rastreabilidade: decision 032.1–032.2 em PROGRESS.
- **Estilo**: importar `references/design-system.css` diretamente — única fonte de tokens e componentes. Tokens de `:root` imutáveis; classes novas permitidas (só com tokens, documentadas em `references/design-system.html`).
- **Testes**: Vitest. Core com TDD obrigatório; `jsdom` só se um teste de UI precisar.
- **Export XLSX**: lib a decidir na issue 019 (candidatas: `exceljs`, `xlsx`/SheetJS; critério: gerar 100% no navegador, zero rede em runtime).
- **Sem** back-end, sem secrets, sem chamadas de rede (spec §10, §11.1).

## Estrutura de pastas (alvo)

```
src/
  core/      # lógica pura: cálculo, validação, parsing/formatação — TDD, sem DOM, sem localStorage
  storage/   # persistência localStorage + backup/restauração JSON
  export/    # XLSX + página de impressão
  ui/        # componentes DOM por tela — zero lógica de negócio
```

## Convenções

- Valor canônico sempre em **gramas**, precisão total; arredondar **só na exibição** (spec §9): % 2 casas · peso 1 casa · R$ 2 casas · custo/g 4 casas.
- **Uma única função central de recálculo** por receita, sempre a partir do estado puro (spec §1.6). Sem botão de enviar; recálculo imediato por campo.
- Entrada numérica aceita vírgula ou ponto; exibição com vírgula; datas `aaaa-mm-dd` (spec §7.1).
- Todo módulo novo: cabeçalho citando as seções da spec que implementa; fórmulas não óbvias comentadas com `§`.
- Commits convencionais (`feat:`, `fix:`, `test:`, `chore:`), sempre com o nº da issue. Nunca push automático.
- **Idioma (decisão do cliente 2026-07-05)**: código, comentários, commits, planos, issues e docs de processo (PROGRESS, este arquivo) podem ser integralmente em inglês. **Exceção**: toda string visível ao usuário final na UI é pt-BR (produto para padeiro brasileiro; formatos da spec §7.1).
- O exemplo validado da Seção 12 é o **teste dourado** permanente da suíte: R$ 8,86 · 70% / 72,7% · 1100 g · 192% · 1041,7 g.

## Mapa de módulos

_(preenchido pelo escriba conforme o código nasce — caminho → responsabilidade → seções da spec)_

| Caminho | Responsabilidade | Seções da spec |
|---------|------------------|-----------------|
| `src/core/types.ts` | Interfaces de domínio: Recipe, Ingredient, Sourdough, BakeEntry, Pricing, etc. Cópia 1:1 da seção 6. Campos derivados marcados readonly. Sem lógica, sem DOM. **Issue 030 (divergência spec v5 aprovada)**: `PackageCost.packageUnit` estreitado para `'g'\|'kg'` (TS rede segurança contra `'mL'`/`'L'` — tipo força compilador rejeitar volume); campo `Ingredient.inputUnit?: 'weight'\|'volume'` **removido inteiramente** (não mais necessário sem alternador UI). Mudança não-compatível: código antigo com inputUnit continua válido (optional), mas novo código TS falha tentar usar `'mL'`/`'L'`. | §6 (estrutura de dados), issue 030 (divergência spec v5) |
| `src/core/format.ts` | Parsing e formatação numérica pt-BR: parseDecimal (aceita vírgula OU ponto), formatPercent/Weight/Currency/CostPerGram (Intl.NumberFormat), formatDate (aaaa-mm-dd). Arredonda SOMENTE exibição (§9). | §6, §7.1, §9 (entrada/exibição) |
| `src/core/format.test.ts` | TDD: 23 casos parseDecimal, formatters, formatDate. Golden §12 validado (1041,7; R$ 8,86). | §7.1, §9, §12 |
| `src/core/bakers.ts` | Baker's percentage — convenção de padeiro. Funções puras: flourTotal (Σ pesos farinhas principal); weightFromPercentage (Peso = F_total × %/100); percentageFromWeight (% = Peso/F_total × 100, guarda F_total ≤ 0); **percentagesSumTo100** (dono único SUM_EPSILON, reusável); flourPercentagesSumTo100 (delega a percentagesSumTo100). Sem DOM, sem arredondamento (§9), precisão total. | §1.1, §1.2, §1.5, §2.A.2, §3.A, §5.C |
| `src/core/bakers.test.ts` | TDD: 22 casos baker's percentage — flourTotal (4), weightFromPercentage (6), percentageFromWeight (4), flourPercentagesSumTo100 (4), **percentagesSumTo100 (2, novo)**, pureza (2). Golden §12 validado (1000g farinha; 700g água, 20g sal, 200g fermento). | §1.1, §1.2, §2.A.2, §3.A, §5.C, §9, §12 |
| `src/core/sourdough.ts` | Sub-receita do fermento natural — refactor-farinhas-multiplas §5.3/§5.5/§5.6 (fase 2). Funções puras: sourdoughTotalWeight (W_ferm = F_total × %/100, reuso weightFromPercentage); **sourdoughFlourProportionSum** (Σ proporções das farinhas do fermento — refactor §5.3); **sourdoughDenominator** (Σ global = Isca + Σfarinhas + Água, novo modelo proporção por linha — refactor §5.3); isValidSourdoughParts (guarda denominador>0 e partes≥0); computeSourdoughWeights (rateio interno W_ferm × proporção ÷ denom, hidratação DERIVADA = água ÷ Σfarinhas); distributeSourdoughFlourWeights (rateio por proporção-share). Modelo antigo (Partes fixas `{isca, flour, water}` + % das farinhas do fermento) REMOVIDO; SourdoughParts agora `{isca, water}` (sem `flour`), SourdoughFlour.proportion substitui percentage. Sem DOM, sem arredondamento (§9), precisão total. | §2.B, §2.B.2, §2.B.3, §3.B, §5.C, refactor-farinhas-multiplas §5.1–§5.6 |
| `src/core/sourdough.test.ts` | TDD: 12 casos sourdough — sourdoughTotalWeight (1), partsSum (1), computeSourdoughWeights (5 limites), isValidSourdoughParts (1), distributeSourdoughFlourWeights (2), sourdoughFlourPercentagesSumTo100+pureza (2). Cobre hidratação derivada (null quando FarinhaFerm=0), valores crus (§9), transição entre Partes e pesos. | §2.B, §2.B.2, §2.B.3, §3.B, §5.C, §9 |
| `src/core/hydration.ts` | Painel de hidratação e Farinha Real Consumida. Funções puras: declaredLiquidsWeight (Σ category 'liquid' sem 'fat'); nominalHydration (ΣLíquidos/F_total×100, null se ÷0); realHydration ((ΣLíquidos+ÁguaFerm)/(F_total+FarinhaFerm)×100, null se ÷0); realFlourConsumed (F_total+FarinhaFerm, sempre número). Sem DOM, sem arredondamento (§9), precisão total. Reusa flourTotal (bakers.ts, §3.A). | §2.C, §2.D, §5.C, decisão 15 |
| `src/core/hydration.test.ts` | TDD: 14 casos hidratação — declaredLiquidsWeight (4, fat excluído), nominalHydration (3), realHydration (4, sourdough=null, ÷0 defensivo), realFlourConsumed (3). Golden §12 validado (70% nominal, 72,7272…% real, 1100g farinha real). Cobre edge case F_total=0 com fermento>0 (Nominal null, Real numérico). | §2.C, §2.D, §5.C, §9, §12, decisão 15 |
| `src/core/costs.ts` | Cálculo de custos — núcleo puro. Funções: **packageSizeInGrams** (normaliza unidades para gramas), costPerGram (Preço÷Peso, derivado nunca digitado, null se Peso≤0); ingredientRecipeCost (peso×C/g, propaga null); sourdoughCost (Σ FarinhaFerm×C + ÁguaFerm×C, Isca SEMPRE fora); sourdoughCostPerGram (Custo÷W_ferm, 0 se inválido); totalRecipeCost (Σ ingredientes+fermento com soma compensada Neumaier). Sem DOM, sem localStorage, precisão total. Reusa costPerGram base. **Issue 030 (divergência spec v5 aprovada)**: `packageSizeInGrams` reduzido — apenas `'kg'`×1000 e `'g'`×1 (casos `'L'`/`'mL'` removidos, densidade 1:1 não mais aplicável). Comentário de cabeçalho atualizado (§2.A não se aplica mais a volume). | §2.A, §2.A.1, §2.B.2, §3.E, §5.C, issue 030 (divergência spec v5) |
| `src/core/costs.test.ts` | TDD: 13 casos custo — packageSizeInGrams (1), costPerGram (4, derivado, null em Peso≤0), ingredientRecipeCost (1, propaga null), sourdoughCost (3, Isca fora), sourdoughCostPerGram (1, divide segura), totalRecipeCost (2, golden §12 → 8,86 exato), pureza (1). Golden §12 validado (R$ 8,86 com Neumaier; azeite 0,064/g para 1250g a R$80). | §2.A, §2.A.1, §2.B.2, §3.E, §5.C, §9, §12 |
| `src/core/pricing.ts` | Precificação — núcleo puro. **Issue 041 (2026-07-06): semântica sobrescrita de margem-sobre-preço para markup-sobre-custo.** Funções: effectiveQuantity (≥1, guarda ÷0); unitCost (CustoTotal/Qtd derivado §3.E); **priceFromMargin (modo % de Lucro, Preço = Custo×(1+p/100), p ≥ 0 sem teto)**; priceFromSalePrice (modo Preço Fixo, profitMargin = lucro/custo×100, denominador custo com guarda ÷0); priceFromProfit (modo Lucro Fixo, profitMargin = lucro/custo×100, denom custo); pricingTotals (totais §3.E — totalProductionCost = unitCost×Qtd, golden §12 fonte da verdade); marginStatus (faixas >30/15-30/<15 §4); isLoss (break-even inclusivo ≤ §5.C). **Removidos**: `MARGIN_MAX` (99,9, não existe mais divisor `1−m/100`), `clampMargin` (função deletada). **Mantidos**: `MARGIN_MIN = 0` (piso). Sem DOM, sem localStorage, precisão total (§9). | §3.E, §4, §5.C, §12, issue 041 (sobrescreve decisão 4) |
| `src/core/pricing.test.ts` | TDD: 9 casos precificação (issue 041 recomputado). Casos: priceFromMargin markup-sobre-custo (2: simples custo 5→preço 6, alto p=200 sem explodir), priceFromSalePrice/priceFromProfit (2 cada, denominador custo com guarda ÷0), sincronização 3 modos (1), pricingTotals (1), marginStatus (6 cores), isLoss (1), pureza (1). Removidas asserções de `clampMargin`/`MARGIN_MAX`. **Golden §12 recomputado (issue 041)**: unitCost=4,43, p 40 → salePrice ≈ 6,202/profitPerUnit ≈ 1,772 (não 7,3833/2,9533 antigo), totals 8,86/12,404/3,544 (novo gabarito). | §3.E, §4, §5.C, §9, §12, issue 041 |
| `src/core/scaling.ts` | Escalonamento por peso alvo + fornada por unidade — núcleo puro. Funções: recipeSumPercent (Σ %ingredientes + %fermento, decisão 3 — fermento ENTRA); scaledFlourTotal (F_nova = W_alvo/(SomaReceita%/100) §3.D passo 2, guardas null); applyTargetScaling (ação explícita §1.6 clona Recipe ou null, suporta âncoras 'total'/per-unit §2.E.1, reusa effectiveQuantity). Sem DOM, sem localStorage, precisão total. Reuso: recipeSumPercent/scaledFlourTotal base para ação; effectiveQuantity(pricing.quantity) garante N ≥ 1. | §1.6, §2.E.1, §3.D, §12, decisões 3, 16 |
| `src/core/scaling.test.ts` | TDD: 11 casos escalonamento — recipeSumPercent golden/fermento-0 (2), scaledFlourTotal alvo/soma/limites (3), applyTargetScaling pesos/per-unit/guards/pureza (6). Cobre ambas âncoras 'total'/'per-unit', edge cases alvo≤0/soma≤0 → null, modo peso→% indisponível. Golden §12: F_nova para alvo 2000 ≈ 1041,67, pesos escalonados batem, per-unit N inalterado. | §1.6, §2.E.1, §3.D, §5.C, §9, §12, decisões 3, 16 |
| `src/core/recalc.ts` | **Engine central de recálculo em lote** — UMA função `recalculate` que reconstrói TODOS os valores derivados de uma Recipe a partir do estado puro (§1.6). Orquestra sem duplicar: bakers (§3.A) · sourdough (§3.B) · hydration (§2.C/§2.D) · costs (§3.E) · pricing (§3.E/§3.E decisão 4). Suporta 2 modos: (a) %→peso — derivar pesos a partir das %, reusando bakers.weightFromPercentage; (b) peso→% — calcular % como peso/total geral (INCLUI W_ferm §1.3), reusando bakers.percentageFromWeight; implementa transitionToPercentageMode (§1.5) para conversão explícita. **Extensão issue 009 (per-unit)**: em modo %→peso com batchPlanningMode='per-unit' (§2.E.1), F_total = flourPerUnit × N onde N = effectiveQuantity(quantity) ≥ 1; peso→% força batchPlanningMode='total' (§2.E.1: planejamento por unidade só em %→peso). Sem DOM, sem localStorage, sem arredondamento (§9), pureza total. Tipos ampliados: HydrationSummary/RecipeSummary com `number | null` (contrato null≠0); Pricing.priceInputMode novo (`'sale-price'|'margin'|'profit'`). | §1.2, §1.3, §1.5, §1.6, §2.E.1, §3.A, §3.B, §3.E, §2.C, §2.D, §9 |
| `src/core/recalc.test.ts` | TDD: 8 casos engine central — modo %→peso (deriva pesos de %), modo peso→% (denominador fermento+principal), transição peso→%→peso (F_total dos pesos atuais), idempotência ambos modos, pureza (entrada não mutada), null defensivo (custo impossível não colapsa a 0), **per-unit sem escalonamento** (F_total = flourPerUnit × N, pesos ≡ golden §12). Golden §12 validado ponta a ponta (unitCost 4,43; preço 7,3833; margem 40; hidratação real 72,7273%; farinha real 1100g). | §1.2, §1.3, §1.5, §1.6, §2.E.1, §3.A, §3.B, §3.E, §2.C, §2.D, §5.C, §9, §12 |
| `src/core/golden-example.test.ts` | **Teste dourado permanente** — fixa gabarito da §12 como contrato permanente da suíte, exercido por UMA chamada de `recalculate` sobre estado puro (§1.6, §9). Antes: placeholder que falhava. Agora: real com 5 asserts (unitCost, salePrice, profitMargin, realHydration, realFlourConsumed). Gabarito §12: 1000g farinha, 200g fermento (H 100%), 70% hidr. nominal, 72,7% real, 1100g farinha consumida, R$ 8,86 custo, 2 unidades 40% margem → 7,3833 preço, 2,9533 lucro unit. | §12 (exemplo validado) |
| `src/core/validation.ts` | **Camada de validação** — funções puras traduzindo predicados já existentes em {valid, level, message} pt-BR (§7.1), distinguindo bloqueio (reverte/impede UI) de aviso (permite/sinaliza). Contrato ValidationResult = ValidationIssue\|null (null=OK). Regras: validatePercentageSum (§5.A), **validateFlourPercentageSumSoft** (refactor §4 — aviso não-bloqueante, fase 1 soma soft das farinhas principais), validateFlourCount (§5.B), validateProductQuantity/NonNegative (§5.C), validateSourdoughParts/FlourPart/Proportion (§5.B/§5.C, refactor §5.6 sem "somar 100" fermento), **validateMargin (§5.C, issue 041: bloqueia só negativo, sem teto 99,9%)**,  PriceVsUnitCost/**PackageSize** (§5.C), validateQuantityProduced/QuantitySold (§5.C/§5.D), validateBakeDate (§14.6). `today` injetado (nunca `new Date()` interno). Validação sobre valores CRUS (§9 só exibição). Sem DOM, sem localStorage, pureza total. Reusa donos únicos: percentagesSumTo100 (bakers), sourdoughFlourProportionSum/isValidSourdoughParts (sourdough), isLoss (pricing), formatDate (format). **Desvio consciente da spec**: §2.A/§5.A (trava-100% bloqueante) só para farinhas do fermento; farinhas principais usam validateFlourPercentageSumSoft (aviso em batchPanel.ts). **Issue 030 (divergência spec v5 aprovada)**: mensagem de `validatePackageSize` simplificada para "Peso do produto deve ser maior que zero." (sem "/Volume" — volume não existe mais). **Issue 041 (sobrescreve decisão 4)**: `validateMargin` agora reusa `MARGIN_MIN` (piso 0), bloqueia só `margin < 0` (removido teto). | §5.A, §5.B, §5.C, §5.D, §14.6, §7.1, §10, §11.1, refactor-farinhas-multiplas §4, issue 030 (divergência spec v5), issue 041 |
| `src/core/validation.test.ts` | TDD: 15 casos validação — percentageSum §5.A (2: principal + fermento), flourCount §5.B (2), productQuantity §5.C (2), nonNegative §5.C (1), sourdoughParts §5.B/§5.C (2), sourdoughFlourPart aviso §5.B (1), sourdoughProportion §5.C (1), margin §5.C (1), priceVsUnitCost §5.C (1), packageSize §5.C (1), quantityProduced §5.C (1), quantitySold §5.D (2), bakeDate §14.6 (1). Cobre bordas felizes com golden §12; garante nenhuma redistribuição/mutação (§5.A literal). | §5.A, §5.B, §5.C, §5.D, §14.6, §7.1, §9, §12 |
| `src/storage/local.ts` | **Abstração de storage** — interface StorageLike (subconjunto Web Storage: getItem/setItem/removeItem); createMemoryStorage (Map para testes, sem jsdom); **defaultStorage** (acessor único ao localStorage real). Vitest node-safe (zero jsdom dep). Único ponto de amarração ao browser. | §10 (persistência 100% local) |
| `src/storage/recipes.ts` | **Store CRUD de receitas** — createRecipeStore orquestrando list/get/create/update/rename/duplicate/remove sobre Recipe[] persistida em localStorage (chave mp.recipes.v1 versionada). Serializa JSON nativo; reviver dirigido SÓ createdAt/updatedAt (nunca coage strings usuário a Date). State "cru" — NÃO recalcula derivados na carga (§1.6 responsabilidade core). I/O e tempo injetados (now, newId) — determinismo teste. newId padrão = crypto.randomUUID (secure context OK, fallback injetável). **+replaceAll (issue 012)**: substitui total do conjunto preservando id/datas originais (sem regenerar). **Issue 030 (divergência spec v5 aprovada)**: função exportada nova `migrateVolumeUnits(recipe): Recipe` — relabel puro `'L'→'kg', 'mL'→'g'` em todos os `packageCost` (ingredients, sourdough.waterPackageCost, sourdough.flours[].packageCost), packageSize numérico **intocado** (densidade 1:1), remove campo `inputUnit` de cada ingrediente se presente. Idempotente (receita sem volume passa inalterada). Chamada em `reviveDates` após `migrateSourdough` ao carregar receitas do localStorage. | §2.F (estado completo, duplicação), §6 (modelo Recipe), §10 (persistência), §11.1 (zero secret), issue 030 (divergência spec v5, nova migração) |
| `src/storage/recipes.test.ts` | TDD: 10 casos CRUD — list/get (1 each), create com seed/vazio (2), update idempotente (1), rename (1), duplicate clona completo (1), remove (1), round-trip JSON reviver (1), newId injetável (1). | §2.F, §6, §10 |
| `src/storage/prefs.ts` | **Store de preferências globais** — createPrefsStore persistindo toggle único showCosts (pref global, não per-receita §2.A.2) em localStorage (chave mp.prefs.v1). Default false (custos ocultos). JSON corrompido/ausente → default sem crash. | §2.A.2 (toggle custos), §10 (persistência) |
| `src/storage/prefs.test.ts` | TDD: 3 casos pref global — default getShowCosts()=false (§2.A.2 oculto), setShowCosts(true) persiste, round-trip. | §2.A.2, §10 |
| `src/storage/backup.ts` | **Backup/restauração de TODOS os dados do usuário** — 2 funções puras (exportBackup — monta envelope {app, schemaVersion, exportedAt ISO §7.1, recipes, bakeHistory}, serializa JSON; importBackup — valida ANTES de escrita, revive datas dirigidas, throw pt-BR em erro). 2 orquestradores (collectBackupData — reúne recipes + bakeHistory seam mp.bakes.v1; applyBackupData — SUBSTITUIÇÃO TOTAL via replaceAll + setItem). 2 helpers DOM browser-only sem wiring node (downloadBackupFile, readBackupFile). Seam **BAKES_STORAGE_KEY** exportado (issue 013 importa). Zero rede, zero secret, validação ANTES de qualquer escrita (por construção, testado). **Issue 030 (divergência spec v5 aprovada)**: `reviveRecipeDates` (antes: só `migrateSourdough`) agora chama `migrateVolumeUnits` após `migrateSourdough` ao restaurar backup (GAP 2 corrigido — backup agora faz migração de volume). | §10 (backup/restaurar local), §11.2 (formato migração), decisão 11, §7.1, issue 030 (divergência spec v5, migração em restore) |
| `src/storage/backup.test.ts` | TDD: 12 casos backup — exportBackup vazio/clock/pureza (3), importBackup round-trip/JSON ruim/sem envelope/versão (4), applyBackupData substituição+coleta (2), estado intacto (2). Cobre validação antes de escrita (falha não corrompe), bakeHistory ausente → [] sem crash. | §10, §11.2, §7.1, decisão 11 |
| `src/core/bakes.ts` | **Núcleo puro do histórico de fornadas** — 17 funções puras (spec §14.3–§14.7, issue 045 aba-balanco §2). Por-fornada (§14.3): bakeTotalCost/Revenue/Profit/Wastage/WastageRate (guarda ÷0→null); computeBakeDerived (clona e preenche 5 derivados snapshot). Planejadas (§14.6): isPlanned, confirmPlanned (deleta chave, passa a contar em agregações). Agregações (§14.4): aggregatePeriod (filtra planned antes; wastageRate/averageProfitMargin = agregados PONDERADOS com guarda ÷0→0); groupBy genérico dirigido por chave/bounds; groupByDay/Week/Month (formatDate chaves, sem date-fns, mondayOf literal seg–dom). Filtros (§14.5): filterByRecipe, filterByDateRange (inclusivo bordas). Comparação (§14.5): percentVariation (anterior 0→null), comparePeriods (métrica-a-métrica). Melhor/pior (§14.5): bestPeriod/worstPeriod (lucro). Órfãs (§14.7): isOrphan (SEM cascade delete). **Display (issue 045)**: `bakeStatus(totalRevenue, totalCost): number|null` — status F/C×100 por fornada e agregado ΣF/ΣC (contrato null≠0: C≤0→null, não 0/NaN), dono único da métrica, reusado por historyView.ts aba BALANÇO. Sem DOM, sem localStorage, pureza total, Date nativo (MDN). | §14.3, §14.4, §14.5, §14.6, §14.7, §9, issue 045 (bakeStatus F/C×100) |
| `src/core/bakes.test.ts` | TDD: 16 casos bakes core — computeBakeDerived (2), wastageRate §14.3 (2), isPlanned/confirmPlanned (2), aggregatePeriod §14.4 (3, filtra planned), groupByDay/Week/Month §14.4 (3, chaves lexicográficas), filterByRecipe/filterByDateRange §14.5 (2), percentVariation/comparePeriods §14.5 (2), bestPeriod/worstPeriod §14.5 (1), isOrphan §14.7 (1), pureza (2). Cobre edge cases: período vazio, planned=true filtrado, anterior=0→null, empate best/worst. | §14.3, §14.4, §14.5, §14.6, §14.7, §9, §12 |
| `src/storage/bakes.ts` | **Store CRUD de fornadas em localStorage** — createBakeStore orquestrando list/get/listByRecipe/create/update/remove/**replaceAll** (backup issue 012) sobre BakeEntry[] persistida em localStorage (chave BAKES_STORAGE_KEY importada de backup.ts — fonte única, seam integrada). Serializa JSON nativo (Date→ISO toJSON), reviver dirigido SÓ date (nunca coage recipeName/notes usuário a Date). State "cru" — sem recalc derivados (core responsabilidade). I/O/tempo injetados (now, newId). **+replaceAll**: restauração sem cascade (§14.7), preserva id/date crus. Zero rede, zero secret (§10, §11.1). Molde exato de recipes.ts (regra de ouro 2). | §6, §10, §14.7, decisão 11 |
| `src/storage/bakes.test.ts` | TDD: 7 casos bakes storage — list (1), get/listByRecipe (2), create (1, seed/defaults), update (1, idempotente), remove (1), replaceAll (1, restauração sem cascade). Backend createMemoryStorage() (sem browser, Vitest node). Clock/newId injetados (determinismo). | §6, §10, §14.7 |
| `src/ui/dom.ts` | **Helpers DOM seguros** — `h(tag, attrs, children)` cria elementos via `createElement`/`setAttribute`/`textContent` (NUNCA `innerHTML` com string bruta, escape XSS regra 3). `clear(node)` limpa sem `innerHTML = ''`. `on(node, evt, fn)` wrapper tipado de `addEventListener`. Único ponto que toca DOM cru nesta tela. Sem fórmula, sem localStorage. | §10 (app offline), §11.1 (zero secrets) |
| `src/ui/modal.ts` | **Primeiro componente modal genérico do design system (issue 035, exceção explícita de decisão 022 "sem modal")** — `openPromptModal(options: PromptModalOptions)` cria overlay+caixa diálogo via `h`/`on` (dom.ts, nunca `innerHTML`), APENAS para fluxo "+ Nova receita" (recipesList.ts). Componente genérico sem lógica receita: `{title, label, confirmLabel, cancelLabel, emptyError?, onConfirm}`. Comportamento: foco preso entre input/botões (cíclico), Esc/click-backdrop/Cancelar fecham, Enter/blur confirmam se trim-não-vazio (senão sinaliza `.form-status--error` reuso design-system, retem foco), foco restaurado ao gatilho ao sair. Contrato acessibilidade full (role="dialog", aria-modal="true", aria-labelledby, labels). **Decisão técnica (issue 035.3)**: div manual vs <dialog> nativo — jsdom não implementa HTMLDialogElement.showModal(), tornando cenários Esc/backdrop/foco-preso não-testáveis; div manual replica 100% contrato em jsdom sem lib nova (zero dependência). | §2.F (criar receita), §10 (app offline), §11.1 (XSS regra 3), issue 035 (1º modal exceção, div manual vs <dialog>) |
| `src/ui/inlineNameEdit.ts` | **Mecânica genérica de edição inline de nome** (issue 036, reuso regra de ouro 2) — `startInlineNameEdit(opts)` função desacoplada de store, extraída de `startInlineEdit` em recipesList.ts (issue 033). Parametrizações `makeDisplay(name)` (recria nó de exibição via `h`/dom.ts, escape XSS automático), `onCommit(newName)` (callback persistência), `onDisplayChange?` (notifica novo nó ao caller). Comportamento idêntico ambos os usos (card receitas + header Calculadora): tríplice guarda (vazio/sem mudança NÃO grava), Enter/blur confirmam, Esc cancela, flag `settled` evita reprocessamento blur pós-Enter. Reuso total regra 2: `h/on` dom.ts (zero lib nova, regra 1), sem duplicação mecânica. **Aplicação issue 036**: Calculadora monta `<h1 id="recipe-name" tabindex="0" role="button" aria-label="Editar nome da receita">` com listeners click/keydown (Enter/Space=editar), `onCommit = store.update + autosave`, preserva semântica `<h1>` (não `<button>`, respeitando mockup). **A11y issue 036.2**: achado alto guardiao-design (falta Enter/Space gatilho teclado em <h1>) corrigido nesta iteração — tabindex/role/aria-label + keydown pattern MDN, teclado completo (Enter/Space/Escape). | §4.1 (célula editável), §11.1 (XSS regra 3 dom.ts, a11y Enter/Space), regra de ouro 1–2 (reuso sem lib nova, zero dup), issue 036 (extração genérica, a11y teclado corrigido) |
| `src/ui/seed.ts` | **Estado inicial golden** — `goldenSeed()` retorna Recipe crua (sem derivados — `recalculate` preenche) reproduzindo gabarito §12 (Farinha 1000g/100%, Água 700g/70%, Sal 20g/2%, Fermento 20%). **Issue 035 AC2**: Azeite removido da seed (era 40g fat, mockup antigo) — nova seed = Pão Rústico com 5 ingredientes principais sem fat pré-preenchido. **Refactor-farinhas-multiplas §5.7 (fase 2)**: Partes do fermento `{isca:0, water:1}` (sem `flour`), farinha do fermento com `proportion:1` (era `percentage:100`) — denominador global 0+1+1=2 preserva valores §12 gabarito. Dados puros até existir fluxo "abrir receita" (issue 017 receitas). **Issue 030 (divergência spec v5 aprovada)**: Água (receita + fermento) `packageUnit` vira `'kg'` (era `'L'`, mesmo `packageSize:1` = 1000g; relabel puro, não conversão); `inputUnit:'weight'` removido (campo deixou de existir). Golden §12 números **inalterados** (1041,7g, R$ 8,86). | §12 (exemplo validado), §6 (modelo Recipe), §2.F (criar receita), refactor-farinhas-multiplas §5.7, issue 030 (divergência spec v5, packageUnit relabel), issue 035 (remoção Azeite seed) |
| `src/ui/state.ts` | **Estado da Calculadora em memória** — `createAppState(recipe, prefs): AppStateStore` interface (getState, update, subscribe, showCosts) — orquestra clone+`recalculate` (008)+notify (§1.6). `update(mutator)` clona estado, aplica mutação, roda recalc, notifica assinantes. `showCosts` lido/escrito via PrefsStore (011, reuso regra 2). Sem fórmula. | §1.6 (recálculo imediato), §2.A.2 (toggle custos) |
| `src/ui/ingredientsTable.ts` | **Tabela de Insumos (card Ingredientes)** — `renderIngredientsTable(root, store)` monta thead **7 colunas fixas** (§2.A.2: Ingrediente·%·Peso·Preço Pago·Peso do Produto·Custo/g·Custo na Receita), tbody com **contrato-espelho refactor-farinhas-multiplas §3** (fase 1): linhas de farinha (`category:'flour'`) renderizadas PRIMEIRO (`buildFlourDisplayRow`), formando bloco CONTÍGUO no topo — sincronizadas READ-ONLY com tabela "Farinhas" de batchPanel.ts (nome/proporcenagem/peso/custo) com rótulo "(vem da Ancoragem)"; demais ingredientes + linha Fermento (edição, sem remover farinhas), tfoot total. Toggle "Exibir custos" oculta 4 colunas financeiras (default false, persiste prefs 011). Edição inline: `input` → parseDecimal+store.update → recalc via subscribe (never recriar input focado); blur valida (010) reverte+erro. Add/remove com guard §5.B (mín 1 farinha, migrado para batchPanel). Derivados renderizados via `textContent` SEM box (decisão 24, brandbook §4.1). Escape XSS tudo via dom.ts. Reuso: recipeSumPercent (scaling 009 §3.D), formatters (002), validadores (010). Zero fórmula — tudo lido de store.getState(). **Issue 030 (divergência spec v5 aprovada)**: coluna "Unidade" removida inteira (thead + 4 row builders + colspans recalculados 9→8/2→1) — sem volume, toda linha é sempre "g", coluna redundante (brandbook §4.1 minimalismo); `buildUnitToggle` removido (alternador g/mL não existe mais). | §2.A.2, §4, §5.B, §5.C, §7.1, §9, refactor-farinhas-multiplas §3, issue 030 (divergência spec v5, coluna/toggle removidos) |
| `src/ui/ingredientsTable.test.ts` | **Testes jsdom da Tabela** — 4 casos TDD (environment jsdom file-level): (1) renderIngredientsTable nome=`<script>alert(1)</script>` → sem nó `<script>` (escape XSS regra 3); (2) input % Água 70→80 → Peso exibe 800,0 na hora (recálculo vivo §1.6); (3) toggle custos → class/prefs refletem (011); (4) blur % que quebra 100% (2 farinhas) → revertido (§5.A). Justificativa jsdom (architecture: "só se teste UI precisar"): XSS + wiring recálculo + toggle + revert são críticos (baratos de cobrir, invalida regressão). | §1.6, §2.A.2, §4, §5.A, §11.1 |
| `src/ui/pages/calculadora.ts` | **Entry da página Calculadora** (index.html) — carrega design-system.css, instancia PrefsStore (011), seed via goldenSeed(), `createAppState`, monta nav/header/card, chama renderIngredientsTable, `subscribe` para repintar. Issue 014: card Ingredientes implementado. Issue 016: painéis Hidratação/Precificação/Escala/Sub-receita. | §1–6, §9, §10 (app MPA client-side), §2.A.2 |
| `src/ui/cellHelpers.ts` | **Helpers de célula compartilhados entre tabelas** — extração de 014 sem duplicação (regra 2): `applyValidation` (bloqueio reverte + erro/aviso §5), `moneyPlain` (moeda sem "R$" para input editável mockup), `UNIT_OPTIONS` (opções unidade por categoria). Reusado por sourdoughTable (015) sem reimplementação. **Extensão issue 017**: novo `marginChipClass(status: MarginStatus): string` (ponto único mapa MarginStatus → `.chip-ok/.chip-warn/.chip-crit`, antes duplicado como `CHIP_CLASS` em pricingPanel.ts — agora reusado por pricingPanel 016 e recipesList 017, regra 2). **Issue 030 (divergência spec v5 aprovada)**: `UNIT_OPTIONS` estreitado — `liquid` e `fat` agora apenas `['kg','g']` (antes líquidos tinham `['L','mL']`, gorduras `['g','kg','mL','L']`; foi mudança em cascata: ingredientsTable.ts/sourdoughTable.ts/batchPanel.ts herdam automaticamente). | §2.A.1, §4, §5, §7, issue 030 (divergência spec v5, unidades estreitadas) |
| `src/ui/sourdoughTable.ts` | **Tabela vertical da Sub-receita Fermento** — `renderSourdoughTable(root, store, editedCostIds)` monta card "Composição do Fermento" (§2.B) **refactor-farinhas-multiplas §5.1–§5.6 (fase 2)** — tabela com 5 linhas em ordem: (1) **Total de fermento** (read-only, W_ferm consumido de % Fermento da tabela Ingredientes); (2) **Isca** (proporção editável, custo zero); (3) **Farinhas** (0+, criadas via "+ farinha do fermento", nome + proporção + custo editáveis, removíveis); (4) **Água** (proporção + custo editáveis, não removível); (5) `tfoot` **Total** (Σ proporções via `sourdoughDenominator`, Σ peso = W_ferm, Σ custo). **Novo modelo proporção por linha** (refactor §5.3): peso_linha = W_ferm × proporção ÷ Σproporções (antes Partes fixas + % das farinhas). Colunas análogas a Ingredientes: Componente·Proporção·Peso·Preço Pago·Peso do Produto·Custo/g·Custo; últimas 4 sob toggle §2.A.2. Sem regra "somar 100" fermento (refactor §5.6/AC23). Herança custo (§4): vinculação farinha-fermento ↔ farinha-principal via `inheritSourdoughFlourCosts` (helper puro) + `editedCostIds` Set memória (não persistido, débito 024). Edição custo: input → parseDecimal + store.update + normalize hook (state.ts) → herança + recalc. Zero fórmula — `recalculate` (008) entrega tudo, UI formata. Escape XSS dom.ts, reuso cellHelpers/formatters/validation/sourdoughDenominator. | §2.B, §2.B.1–§2.B.5, §2.A.2, §4, §5, §7, §9, refactor-farinhas-multiplas §5.1–§5.6 |
| `src/ui/sourdoughTable.test.ts` | **Testes jsdom da Sub-receita Fermento** — 9 casos TDD (environment jsdom): renderização vs state (3: peso/partes/resumo), edição custo herança (1), custo nulo (1), toggle visibilidade (1), hidratação derivada (1), custo/kg trivial (1), fixture typo spec (1 — 315 não 310, revisor confirmou §2.B.2). Mesma montagem 014 (createMemoryStorage + PrefsStore + createAppState) com terceiro parâmetro normalize (state.ts issue 015) amarrando inheritSourdoughFlourCosts. | §2.B, §2.B.2, §2.B.3, §2.A.2, §4, §5, §7, §9, §12 |
| `src/ui/hydrationPanel.ts` | **Painel de Hidratação + Farinha Real Consumida** — `renderHydrationPanel(root, store)` monta card "Hidratação" (§2.C) com `.metric-pair` lado-a-lado (Nominal·Real), mais indicador Farinha Real Consumida (§2.D) visível sse W_ferm>0. Valores prontos de `summary.hydration.nominal`/`.real` e `summary.realFlourConsumed` (recalculate 008, funções pure hydration 005), UI formata (formatPercent/formatWeight 002) e exibe "—" se core devolve null (§5.C ÷0 defensivo). Recálculo imediato via subscribe. Zero fórmula aqui. | §2.C, §2.D, §5.C, §9, §12 |
| `src/ui/hydrationPanel.test.ts` | **Testes jsdom do Painel de Hidratação** — 3 casos TDD (environment jsdom): Nominal/Real ratio corretos vs golden §12 (70%/72,73%, 1100g farinha real) (1), Farinha Real visível sse W_ferm>0, "—" se zero (1), formatWeight sem agrupamento milhar (1100,0 não 1.100,0, §9 gabarito 1041,7) (1). Mesma montagem 014 + hook normalize. | §2.C, §2.D, §5.C, §9, §12 |
| `src/ui/modeToggle.ts` | **Toggle de modo de cálculo** — `renderModeToggle(root, store)` injeta botão "Alternar p/ peso → %" que alterna `calculationMode` (§1.3/§1.4). Gerencia banner sticky obrigatório (§1.3 "sinalização visual obrigatória") inserido/removido de `document.body` e classe `body.mode-alt` para destaque `.mode-alt .cell-input.pct` (design-system.css). Entrar em peso→%: simples `store.update`; voltar: `transitionToPercentageMode` (recalc.ts 008, reuso regra 2) via novo `store.applyTransform` (state.ts). Self-contained: gerencia própria reatividade via `store.subscribe`, montado uma vez em batchPanel (016) sem duplicação assinatura. Sem fórmula, sem localStorage. | §1.3, §1.4, §1.5, §2.E.1 |
| `src/ui/modeToggle.test.ts` | **Testes jsdom do Toggle de Modo** — 2 casos TDD (environment jsdom): toggle entra/sai peso→%, banner aparece/desaparece, classe `body.mode-alt` lançada/retirada. Montagem padrão (createMemoryStorage + PrefsStore + createAppState + seed). | §1.3, §1.4, §1.5 |
| `src/ui/batchPanel.ts` | **Ancoragem e Planejamento da Fornada** — `renderBatchPanel(root, store)` monta card (§2.E/§2.E.1) com **tabela "Farinhas" (refactor-farinhas-multiplas §2, fase 1)** — N farinhas principais editáveis (Nome/Proporção/%/Peso/Preço Pago/Peso do Produto/Custo/[Ações]), colunas análogas a Ingredientes MENOS "Unidade" (farinha é sempre g). Botão "+ farinha" adiciona, botão remover por linha (mínimo 1). Proporção editável em `percentage-to-weight` (nunca trava-100%, **desvio consciente da spec §2.A/§5.A** — fase 1 soma soft com aviso chip não-bloqueante `validateFlourPercentageSumSoft`); em `weight-to-percentage` proporção derivada readonly. Mudança estrutural (add/remove/modo) dispara `fullRenderFlours()`; outras mutações repintam derivados/tfoot via `patchFlourDerived()`. Contrato-espelho (fase 1 §3): linhas de farinha refletem exatamente em `ingredientsTable.ts` na mesma ordem, sincronizadas. + campos de âncora (Quantidade de Produtos, Farinha por Unidade, F_total). **Hospeda painéis de ação**: `renderModeToggle` + `renderScalePanel` montados uma única vez. Recálculo imediato (§1.6): input→parseDecimal→store.update; blur→applyValidation (cellHelpers.ts 015, reuso). Zero fórmula — todos os números vêm de `recalculate` (008). | §1.6, §2.E, §2.E.1, §3.E, refactor-farinhas-multiplas §2–§4 |
| `src/ui/batchPanel.test.ts` | **Testes jsdom da Ancoragem** — 3 casos TDD (environment jsdom): F_total editável em 'total' mode, derivado readonly em 'per-unit', mudança de modo de planejamento recalcula F_unit dinamicamente. Montagem padrão + state.subscribe. | §1.6, §2.E.1 |
| `src/ui/pricingPanel.ts` | **Painel de Precificação** — `renderPricingPanel(root, store)` monta card (§3.E/§4, issue 042) com chip de status ("Lucro X%", `marginStatus`, pricing.ts 007 reuso) e bloco `.stack` (empilhado verticalmente) com 4 campos em ordem: Custo unitário (1º, read-only, derivado `summary.costPerUnit` via `setDerivedDisplay`/`formatCurrency`) → Lucro unitário → % de lucro (rótulo novo 042, antes "Margem %") → Preço de venda. Trio sincronizado (§1.6): editar um muda `pricing.priceInputMode` + valor cru via `store.update`; `recalculate` (008) reconstrói outros dois. Campo em edição NUNCA sobrescrito por `subscribe` (marca via `activeField`; demais dois repintados). Blur formata (§9) + limpa `activeField`. Validação (010, reuso): % de lucro bloqueia negativo (`validateMargin` — markup sem teto desde 041, só piso 0); Preço avisa se não cobre unitário (`validatePriceVsUnitCost`). Indicadores (§4): margem colorida `.chip-ok/.chip-warn/.chip-crit` (design-system.css); prejuízo (`isLoss`) renderiza chip `.loss`. Totais de produção (Receita, Lucro) seguem em tabela, não duplicam custo (issue 042). Zero fórmula — tudo pronto. | §1.6, §3.E, §4, §5.C, §9, issue 041 (markup), issue 042 (layout empilhado + rótulo + custo read-only) |
| `src/ui/pricingPanel.test.ts` | **Testes jsdom da Precificação** — 4 casos TDD (environment jsdom): sincronização trio preço/margem/lucro (editar um recalcula outros), campo em edição nunca sobrescrito durante subscribe, aviso preço vs custo, status cores chips (ok/warn/crit). Montagem padrão. | §3.E, §4, §5.C, §9 |
| `src/ui/scalePanel.ts` | **Escalonamento por peso alvo** — `renderScalePanel(root, store)` injeta em batchPanel o campo "Escalonar para peso alvo" + botão "Re-escalar" — **ÚNICA ação não-imediata do app** (§1.6, explícita no clique, nunca contínua). Zero fórmula: `applyTargetScaling` (scaling.ts 009) devolve Recipe nova ou null (indisponível em peso→% §3.D "apenas modo %→peso", ou alvo/soma inválidos §5.C). `store.applyTransform` (novo state.ts 016) não muta/notifica se null — "alvo inválido" + "modo errado" silenciosamente não fazem nada, sem duplicar validação. Botão desabilitado em peso→% como reforço visual. Self-contained: gerencia própria reatividade, montado uma vez. | §1.6, §3.D, §5.C |
| `src/ui/scalePanel.test.ts` | **Testes jsdom do Escalonamento** — 2 casos TDD (environment jsdom): alvo válido recalcula F_total, alvo/modo inválido retorna null silenciosamente (sem efeito). Montagem padrão. | §1.6, §3.D, §5.C |
| `src/ui/ingredientsTable.ts` (extensão issue 016) | **Extensão**: campos de % e peso agora editáveis conforme `calculationMode` (§1.2/§1.3) — modo %→peso: % editável, peso readonly; modo peso→%: peso editável, % readonly com classe `.pct` para destaque (decisão 016–2). Lógica: `calculationMode === 'weight-to-percentage'` controla `readOnly`/`disabled` e renderização (`<input>` vs texto). Cabeçalho atualizado: §1.2, §1.3. | §1.2, §1.3 |
| `src/ui/pages/calculadora.ts` | **Entry da página Calculadora** (receitas.html, issue 032 inversão) — carrega design-system.css, instancia PrefsStore (011), seed via goldenSeed() ou `?recipe=<id>` (issue 017 + 040), `createAppState` com normalize hook (015 herança custo), liga auto-save via `recipeStore.update` (debounce §10 + flush antes unload), monta nav/header/exportBar (issue 019+028 XLSX+Print parametrizado), **campo Nome da Receita (issue 040)** input fixo em card entre export+Ancoragem com lazy-create (nome vazio = no-op efêmera, não-vazio = create+replaceUrl) e store.update (receita carregada), chama renderIngredientsTable (014), renderSourdoughTable + renderHydrationPanel (015), renderBatchPanel (016 + toggle modo 008 + escala 009) + renderPricingPanel (016), `subscribe` para repintar. **Issue 025**: `initCalculadora(deps)` função pura com injeções prefs/recipeStore/storage/search/replaceUrl para jsdom testabilidade. Card order: Ingredientes, Hidratação, Ancoragem e Planejamento, Precificação. Reuso: recipeStore CRUD (011), store.subscribe (016 state.ts), dom helpers, formatters, `recalculate` (008). | §1–2 (composição), §1.3, §1.5, §1.6, §2.B, §2.C, §2.D, §2.E, §2.E.1, §2.F, §3.D, §3.E, §4, §8–10 (offline), issue 017 (?recipe), issue 019 (export), issue 025 (deps), issue 028 (print v2), issue 032 (rota), issue 036 (nome inline — intacto em recipesList), issue 040 (nome fixo lazy-create) |
| `src/ui/recipesList.ts` | **Tela Minhas Receitas** — `renderRecipesList(root, deps)` monta barra ações (busca filter, criar/exportar/restaurar), subtítulo dinâmico, status region (aria-live pt-BR), grid de cards (Nome/Data/Custo/Margem + Abrir/Renomear/Duplicar/Excluir) OU estado vazio (`.empty-state` design-system). Cards exibem derivados via `recalculate` (008, custo/margem). **Issue 033 (refactor II)**: Renomear agora é edição inline — clique "Renomear" substitui `<h3>` por `<input class="cell-input">`, Enter/blur confirmam, Esc cancela, flag `settled` evita reprocessamento pós-Enter/Esc, vazio/igual não grava. Nova função `startInlineEdit` (L224–272). Excluir avisa órfãs §14.7 (SEM cascade mp.bakes.v1, 013). Backup: exporta/importa JSON (012) com validação pré-escrita. Deps injetáveis: confirm/navigate/readFile/download (determinismo teste jsdom, vi.fn) — `prompt` removido (issue 033). Reuso: recipeStore CRUD (011), marginChipClass (cellHelpers 017), formatters (002), `recalculate` (008), backup (012), dom helpers (dom.ts 014). Zero fórmula, zero web API (offline §10). | §2.F, §4, §5, §7.1, §9, §10, §14.7, issue 033 |
| `src/ui/recipesList.test.ts` | **Testes jsdom Minhas Receitas** — 23 casos TDD (environment jsdom): (1–6) vazio → `.empty-state`, 1+ cards grid, filtro busca case-insensitive, criar nova com seed, abrir → `receitas.html?recipe=<id>` (issue 032), duplicar novo UUID com independência; (7–14) **renomear inline (issue 033, 8 casos)**: clique → h3 some + input foca, Enter/blur confirmam, Esc cancela, vazio não grava, idêntico não grava, window.prompt nunca chamado, XSS literal; (15+) excluir warn orphans sem tocar mp.bakes.v1 (spy verify), exportar blob JSON, restaurar importa + status OK/erro. Montagem: createMemoryStorage + recipeStore + bakeStore seam + deps vi.fn. Fixture: 3 receitas golden + derivados preenchidos (decisão 017.1). | §2.F, §10, §14.7, decisão 017, issue 033 |
| `src/ui/pages/receitas.ts` | **Entry Minhas Receitas** (receitas.html) · carrega design-system.css, instancia recipeStore (011) + defaultStorage, preenche h1 estático, chama `renderRecipesList(app, deps)` com injeção confirm/navigate/readFile/download. Zero lógica de negócio (composição pura, regra 2). **Issue 033**: `prompt` removido (renomear agora é inline edit). | §2.F, §10, §14.7, issue 033 |
| `src/ui/bakeForm.ts` | **Formulário de Registro/Edição de Fornada** — `renderBakeForm(root, store, deps)` monta card "Registrar Fornada" ou "Editar Planejada" (§14.2/§14.6) com campos: Date (input type="date" parseLocalDate novo §14.2, round-trip local sem UTC), Receita dropdown (recipeStore.list), Quantidade Produzida (input number, ≥1 block §5.C), Quantidade Vendida (input number, ≥0/≤produzida aviso §14.6 §5.D), Status badges (Confirmada/Planejada visual). Blur valida (010) reverte+erro. Confirmar: computa derivados snapshot via `computeBakeDerived` (013) com totalCost/unitCost/unitSalePrice em 2 casas (moneyPlain §14.2), `bakeStore.create`/`bakeStore.update`, fecha. Edição planejada: pré-preenchimento dados crus, data/receita/custo imutáveis, só produzida/vendida editáveis. Cancelar fecha sem salvar. Validação: today injetado (nunca `new Date()` local), data futuro/hoje block §14.6, quantidade validação 010. Zero fórmula — derivados snapshot snapshot da época (novo estado puro §14.2). Reuso: recipeStore.list (011), computeBakeDerived/isPlanned (013), formatters/parseLocalDate (002), validation (010), dom helpers, bakeStore (013 CRUD). | §14.2, §14.6, §5.C, §5.D, §9, §10 |
| `src/ui/trendChart.ts` | **Gráfico de tendência em SVG puro** — `renderTrendChart(root, bakes, metric, period)` monta `<svg>` elemento plotando ponto+polyline (sem Chart.js/uPlot — custo visual vs design-system reuse). Eixo X: dias/semanas/meses (period='day'/'week'/'month' §14.5), eixo Y: métrica (wastageRate %/averageProfitMargin %/unitSalePrice R$ §14.4). **Filtro pré-gráfico**: aggregatePeriod filtra `!isPlanned` antes groupBy (mitiga bug 021: UI nunca plota planejadas — só confirmadas). Planejadas em tabela avec badge "planejada", mas ausentes gráfico. Escala automática min/max Y, padding bordas. SVG namespace via helper svg() novo (dom.ts §10/§11.1). Reuso: svg helper (dom.ts), aggregatePeriod (013 filtro pré), formatters. Zero lib gráfica além SVG nativo, zero API além DOM. | §14.4, §14.5, §9, §10, §11.1 |
| `src/ui/historyView.ts` | **Tela Histórico de Fornadas completa** — `renderHistoryView(root, store, deps)` monta: (1) **Filtros** (Receita dropdown, Período presets 'última semana'/'últimos 30d'/'mês'/'ano'/custom, botão Aplicar); (2) **KPIs card** (4 métricas pré-filtradas `!isPlanned`: Desperdício médio %, Margem média %, Total Produzido kg, Total Vendido kg §14.4 §14.5); (3) **Gráfico** (trendChart integrado, métrica toggle Margem %/Desperdício %/Preço Unitário); (4) **Comparação períodos** (card "Anterior" vs "Período", % variação §14.5 percentVariation — anterior=0→null); (5) **Tabela lista TODAS conforme filtro + planejadas com badge "planejada" laranja §14.6** (colunas Data/Receita/Custo/Produzidas/Vendidas/Margem/Status; Produzidas/Vendidas editáveis inline, Data/Custo/Receita readonly; ordenação cronológica; destaque melhor/pior §14.5 via bestPeriod/worstPeriod); (6) **Ações**: Confirmar planejada (abre bakeForm), Editar (pré-preenchido, só produzida/vendida), Excluir (confirm pt-BR, SEM cascade §14.7). Status region aria-live pt-BR (error/warning/success: período vazio, fornada confirmada, etc.). (7) **BALANÇO (issue 045)**: nova tabela só-leitura montada DEPOIS da tabela editável "Fornadas registradas" (preserva querySelector('table')=1ª), 10 colunas (spec aba-balanco §2.1), corpo `periodFiltered` com datas DESC, planejadas com badge+"—" em colunas venda, tfoot com ΣC/ΣF/ΣSaldo + Status agregado `bakeStatus(ΣF,ΣC)`, cor `.loss` só se Saldo<0, Status neutro. Zero CSS novo. Reuso `periodFiltered`/`currentSummary`/`isPlanned`/`computeBakeDerived`/`bakeStatus`(core/045). **Pills visualização (issue 046)**: barra segmented control `.period-toggle` acima da tabela BALANÇO (3 botões Completa/Unidades/Fornadas, default Completa); handler `setBalanceView` alterna classe `view-completa|unidades|fornadas` no `<table>` → 2 regras CSS `display:none` escondem colunas condicionais `.col-unit` (Custo un./Preço un.) e `.col-bake` (Custo C/Faturamento/Saldo), zero re-render. Zero fórmula — core (013+045) entrega agregações, UI formata. Reuso: bakeStore CRUD (013), aggregatePeriod/groupByDay/Week/Month/filterByRecipe/filterByDateRange/comparePeriods/percentVariation/bestPeriod/worstPeriod/isOrphan (013), bakeStatus (045), marginStatus/marginChipClass (007/017), validation (010), formatters (002 + parseLocalDate), dom helpers, bakeForm (018). | §14.3–§14.7, §4, §5, §7.1, §9, §10, issue 045 (aba BALANÇO F/C×100), issue 046 (pills visualização Completa/Unidades/Fornadas) |
| `src/ui/bakeForm.test.ts` | **Testes jsdom Formulário de Fornada** — 12+ casos TDD (environment jsdom): renderização vazio/pré-preenchido planejada, validação date futuro/quantidade block, blur formata, snapshot 2-casas moneyPlain, confirmar bakeStore.create com computeBakeDerived, cancelar fecha, escape fecha. Montagem: createMemoryStorage + createRecipeStore + createBakeStore + createAppState + deps vi.fn (confirm, now injetados). Fixture: receita golden, bakes planejada e confirmada. | §14.2, §14.6, §5.C, §5.D, §9, §10 |
| `src/ui/historyView.test.ts` | **Testes jsdom Histórico de Fornadas** — 10+ casos TDD (environment jsdom): renderização vazio/com dados, filtro receita, período presets (última semana/30d/mês), tabela data/receita/metadados, edição inline produzida/vendida, confirmação planejada, exclusão sem cascade, comparação período variação, gráfico SVG presente, aria-live status. Montagem: createMemoryStorage + createRecipeStore + createBakeStore + createAppState + historico.ts deps. Fixture: 2+ receitas, 5+ bakes (confirmadas + planejadas), períodos variados. | §14–§14.7, §4, §9, §10 |
| `src/ui/pages/historico.ts` | **Entry Histórico de Fornadas** (historico.html) — carrega design-system.css, instancia createRecipeStore (011), createBakeStore (013 com BAKES_STORAGE_KEY seam), createAppState (goldenSeed), monta nav/header, chama renderHistoryView(app, deps) com injeção confirm/now. Zero lógica negócio (composição pura, regra 2). | §14–§14.7, §10 |
| `src/core/format.ts` (extensão) | **Extensão**: novo `parseLocalDate(dateString: 'aaaa-mm-dd'): Date` cria meia-noite local (new Date(year, monthIndex, day) sem UTC shift — bloqueia bug 021 `new Date('aaaa-mm-dd')` que UTC-deslocaria). TDD: round-trip 'aaaa-mm-dd' ↔ Date ↔ formatDate idempotente, getTime() em offset local não UTC. Usado por bakeForm (014.2) e historyView (filtro datas) como dono único parse data local. | §14.2, §7.1, §9 |
| `src/ui/dom.ts` (extensão) | **Extensão**: novo helper `svg(tag, attrs, children)` cria elementos SVG via `createElementNS('http://www.w3.org/2000/svg', ...)` (não `createElement` que é HTML). Usado exclusivamente por trendChart.ts (018) para plotagem SVG puro sem Chart.js/uPlot (decisão 018.1 lib custo vs design-system reuso). Mesma interface `h()` mas namespace SVG. | §10, §11.1, decisão 018.1 |
| `src/export/download.ts` | **Helpers browser de download** — dono ÚNICO do padrão "Blob → object URL → `<a download>` → click → revoke" (extraído de storage/backup.ts issue 012, regra de ouro 2 zero dup). `downloadBlob(blob, filename)` genérico para qualquer relatório (JSON backup §10, XLSX §8); `workbookToBlob(workbook: ExcelJS.Workbook)` serializa workbook 100% cliente via `workbook.xlsx.writeBuffer()` (offline §10, regra de ouro 4 MDN ExcelJS browser) e embrulha em Blob com MIME oficial OpenXML. Browser-only (Blob/URL/document), sem unit test node — contrato exercido por wiring calculadora/histórico. Zero rede, zero secret (§8, §10, §11.1, regra de ouro 3). Docs MDN consultadas: Blob API, URL.createObjectURL/revokeObjectURL, ExcelJS writeBuffer. | §8, §10, §11.1 |
| `src/export/xlsx.ts` | **Geração de workbooks ExcelJS para RECEITA e HISTÓRICO** — (a) `buildRecipeWorkbook(recipe, summary, {includeCosts})` monta workbook receita com abas Farinhas/Líquidos/Gorduras/Sal-Extras/Fermento (§2.B)/Hidratação (§2.C/§2.D)/Precificação (§3.E), omite seções financeiras sse !includeCosts (pref global showCosts §2.A.2); (b) `buildHistoryWorkbook(entries, period, {includeCosts})` monta histórico com abas "Fornadas" (cronológica §14.3+) + "Resumo do Período" (§14.5 agregados ponderados). **Decisão formato célula**: célula XLSX = NÚMERO + `numFmt` (não string pt-BR) — planilha estruturada recalculável (§8 ganho sobre CSV). Rótulo moeda/unidade em CABEÇALHO coluna, nunca no valor (evita string). `numFmt` por tipo: peso `0.0`, % `0.00`, moeda `0.00`, custo/g `0.0000` (§9). Valor arredondado à exibição (roundTo §9): moneyPlain 2 casas moeda → golden §12 8.86/7.38 exatos relidos buffer. Derivado impossível (§5.C): célula VAZIA (null), jamais 0/NaN (contrato null≠0 core). Reuso total (regra 2): **não recalcula NADA** — consome `recipe`+`summary` de `recalculate()` (core 008) e `entries`+`BakeHistorySummary` de `aggregatePeriod()`+`computeBakeDerived()` (core 013). Única conta local é `roundTo(value, places)` (§9 exibição decimal, core continua precisão total). Zero rede, zero secret (§11.1, regra de ouro 3). Docs MDN consultadas (regra de ouro 4): ExcelJS writeBuffer (cliente). | §2.B, §2.C, §2.D, §3.E, §8, §9, §12, §14.3, §14.5, decisão 019 |
| `src/export/xlsx.test.ts` | **Testes jsdom de XLSX** — 6 casos TDD: `buildRecipeWorkbook` golden §12 exato (Farinha 1000g/Água 700/Sal 20/Fermento 200, ZERO Azeite — fixture pura, não seed global com azeite), com-sem-custos toggle, coluna fermento ausência, hidratação derivada; `buildHistoryWorkbook` agregado período, período vazio; roundTo arredondamento (2, 1, 0 casas). Montagem: createMemoryStorage + createRecipeStore + createAppState golden puro + entries fixture. Verifica buffer XLSX relido via ExcelJS (`Workbook.xlsx.load(buffer)`) contra valores esperados (gabarito nunca alterado). Cobre null defensivo (derivado impossível → célula vazia, não 0/NaN). | §8, §9, §12, §14.5 |
| `src/export/print.ts` | **Views de impressão "Salvar em PDF" — BASE ISSUE 019, REFACTOR ISSUE 028, REFACTOR V2 ISSUE 034, HISTÓRICO V2 ISSUE 043**: 4 funções de render (issue 019 base, 028 split 4 contextos, 034 redesenho layout Calculadora, 043 redesenho layout Histórico). (a) `renderRecipePrintView(root, {recipe, summary})` monta Receita v2 (cards `.sec-card` por seção Farinhas/Líquidos/Sal e Extras/Fermento Natural/Hidratação, table.rt com colgroup alinhado, badge `.pdf-yield` "Rende N pães" no topo via `recipePageV2`, ZERO $). (b) `renderRecipeCostsPrintView(root, {recipe, summary})` monta Custos v2 (base Receita v2 + coluna Custo à direita por seção, Isca sempre R$ 0,00 §2.B.2, demais `.pdf-debit`, subtotais por seção, card "Custo Total" = fornada N + um pão, Precificação depois com Lucro por sinal, alerta prejuízo §4/§5.C). (c) `renderHistoryPrintView(root, {entries, summary})` Fornadas v2 (issue 043: refactor para `pdfPageV2` genérico — cabeçalho `.pdf-head` + badge "N fornadas", seções `.sec-card` com `table.table` interno, rodapé `.pdf-footer`, reuso total classes Calculadora v2). (d) `renderHistoryCostsPrintView` Financeiro v2 (issue 043: idem refactor para `pdfPageV2`, período custos/faturamento/lucro). Helpers v2 (034+043): `pdfPageV2(title, meta, badge, sections)` **novo 043** (genérico cabeçalho v2 + seções + rodapé, reutilizável Receita/Custos/Histórico), `secCard(title, content)` (section + .sec-head/.sec-body), `recipePageV2(title, meta, yieldQty, sections)` **agora wrapper 043** de `pdfPageV2` (manter saída idêntica Calculadora), `rtTable(opts)` (table com table-layout:fixed + colgroup — garante alinhamento vertical %), `sourdoughTable(sd, withCost)` refatorado (Isca→farinhas→Água→Total, pctLabel="Proporção" fermento/"%" demais, reutiliza `ingredientRecipeCost` core sem recálculo). Reuso total (regra 2): não recalcula — consome `recipe`+`summary` de `recalculate` 008, `entries` de `computeBakeDerived` 013, `isLoss` de pricing.ts, `formatProportion` de format.ts, `ingredientRecipeCost` de costs.ts. Escape XSS 100% via `dom.ts h()/textContent` (regra 3). Paleta impressão: tokens `--print-*` (bg/text/border/border-strong/credit/debit/muted) + classes `.pdf-meta/.pdf-head/.pdf-yield/.sec-card/.sec-head/.sec-body/.pdf-footer/.pdf-credit/.pdf-debit/.pdf-muted-row/.pdf-alert` — escopados `@media print #print-root`. Classes `pageCard`/`section()` **deletadas 043** (legacy Histórico v1, sem uso). Formatação §9: formatPercent/formatWeight/formatCurrency/formatCostPerGram/formatProportion (v2 usa `formatProportion` em fermento, até 2 casas, sem zeros à direita). Wiring: calculadora + histórico montam 2 botões cada via `mountPrintButton(actionsRoot, onPrint?, label?)` parametrizado (gate por `showCosts` pref 011). Mockups referência: `mockups/pdf-receita-v2.html` + `mockups/pdf-custos-v2.html` (novos, substituem pdf-refactor.html como fonte oficial v2). Zero rede, zero secret (§8, §10, §11.1). Docs MDN (regra 4): Window.print. | §2.B, §2.B.2, §2.C, §2.D, §3.E, §4, §5.C, §8, §9, §10, §14.3, §14.5, §14.6, issue 019, issue 028, issue 034, issue 043 |
| `src/export/print.test.ts` | **Testes jsdom de impressão — BASE 028, REFACTOR V2 034**: 13 casos TDD (mix antigos + novos v2). (1) `renderRecipePrintView` v2 conteúdo golden (Pão Rústico, 1.000g farinha, 70% hidr nominal, 72,73% real, 1100g farinha real), snapshot `.sec-card` + `table.rt` com colgroup, badge `.pdf-yield` "Rende N pães", ZERO $. (2) `rtTable` layout alinhado (col widths c-name/c-pct 15%/c-wt 18%/c-cost 20%, %/Proporção slot idêntico entre seções, só Fermento tem "Proporção"). (3) `sourdoughTable` v2 (Isca→farinhas→Água→Total com proporção formatada via `formatProportion`, Isca sempre R$ 0,00 §2.B.2, demais ingredientes com `.pdf-debit`). (4) `renderRecipeCostsPrintView` v2 conteúdo (sections com Custo, card "Custo Total" fornada+pão, Precificação com Lucro por sinal `signedMoneyTd`, alerta prejuízo). (5) `renderHistoryPrintView` Fornadas (intacta 028: data/receita/produzidas/vendidas, ZERO $, planejada `tr.pdf-muted-row` cinza). (6) `renderHistoryCostsPrintView` Financeiro (intacta 028: custo/faturamento/lucro por sinal). (7) null≠0 (§5.C): derivado impossível → "—", nunca "R$ 0,00", sem cor. (8a–8d) escape XSS: `ing.name`/`entry.recipeName` = `<b>x</b> <script>alert(1)</script>` → texto literal, nulo `querySelector('script')` (4 casos, cada render v2 cobra XSS). (9) clique-só regressão §8: renderizar views NÃO chama `window.print`; só clique botão. Montagem jsdom: createMemoryStorage + createAppState golden + entries fixture. Verifica colgroup widths, classes v2, proportions formatadas, cores aplicadas, handler injetável. | §2.B, §2.B.2, §2.C, §2.D, §3.E, §4, §5.C, §8, §9, §10, §14.5, §14.6, issue 028, issue 034 |

---

## v1 Completa — Mapa de módulos fechado (2026-07-05 09:10)

**27/27 issues concluídas**, 295 testes 100% verde, 3 telas funcionais + export + backup. Arquitetura estável:

- **Core (9 módulos)**: types·format·bakers·sourdough·hydration·costs·pricing·scaling·recalc — puro, sem DOM, TDD obrigatório, teste dourado §12 permanente.
- **Storage (5 módulos)**: local·recipes·prefs·bakes·backup — persistência localStorage, seam BAKES_STORAGE_KEY integrada, validação pré-escrita, zero rede.
- **UI (13 módulos)**: dom (helpers + extensão svg)·seed·state·ingredientsTable·sourdoughTable·hydrationPanel·modeToggle·batchPanel·pricingPanel·scalePanel + pages calculadora/receitas/historico·recipesList·bakeForm·historyView·trendChart + cellHelpers — zero fórmula, delegam core+storage.
- **Export (3 módulos)**: download·xlsx·print + testes — XLSX/PDF 100% cliente (exceljs 4.4.0), media print CSS, validação achado CRÍTICO 019.

**Helpers únicos** (regra de ouro 2: zero duplicação):
- `marginChipClass` (cellHelpers) — mapa MarginStatus → classe chip, reuso pricingPanel+recipesList.
- `svg()` (dom.ts) — SVG namespace, trendChart.ts SVG puro sem lib.
- `parseLocalDate()` (format.ts) — parse data local sem UTC shift, bakeForm+historyView.
- `downloadBlob()` (export/download.ts) — dono único Blob→URL→click→revoke, recipesList+calculadora.

**Decisão técnica final (issue 020)**:
- **Repaint F_total pós-escalonamento**: `patchDynamic` (state.ts) detecta F_total mudado sem foco, repinta label derivado. Corrige bug real encontrado verificação final: escala muda F_total, pricingPanel+hydrationPanel recalculam, batchPanel não repinta. Teste regressão 9 batchPanel.test.ts valida correção permanente.

**Pendências para humano (de manhã)**:
1. Reconciliar spec §3.E receita/lucro: texto 14,76/5,90 vs código 14,77/5,91 — achado A, PROGRESS topo.
2. Typo §2.B.2 310→315 — confirmado revisor.
3. Seed padrão com/sem azeite — goldenSeed vs teste §12 puro.
4. types.ts null-widening vs §6 — contrato null≠0 confirmação.
5. UI polish 027 — aria-live calculadora, gráfico animação, escapeHtml removal.

---

## Decisões técnicas registradas

| Data | Decisão | Motivo |
|------|---------|--------|
| 2026-07-04 | Vanilla TS, sem framework | Fidelidade aos mockups HTML/CSS; escopo v1 pequeno; spec já em TS |
| 2026-07-04 | Issues locais em `issues/`, não GitHub | Loop noturno sem dependência de rede/permissões do `gh` |
| 2026-07-05 | Caminhos relativos no vite.config.ts | Evita dependência `@types/node` · Vite resolve naturalmente paths da raiz · desvia conscientemente do plano original com `import.meta.dirname + node:path` |
| 2026-07-05 | Intl.NumberFormat ('pt-BR') + halfExpand vs toFixed manual | toFixed erra half-up (ex.: 2.675→2.67 instead of 2.68) · Intl.NumberFormat aplica rounding decimal correto, já entrega vírgula/símbolo/locale · halfExpand = half-up em todo domínio ≥ 0 do app · roundingMode passado explicitamente para auto-documentar §9 |
| 2026-07-05 | percentagesSumTo100 extraído de bakers.ts como dono único de SUM_EPSILON (issue 004) | flourPercentagesSumTo100 delega a percentagesSumTo100; sourdoughFlourPercentagesSumTo100 também reusa · epsilon centralizado 1e-9, anti-drift IEEE-754 · zero duplicação (regra de ouro #2) · testado em 2 novos casos (21–22 bakers.test.ts) |
| 2026-07-05 | Soma compensada (Neumaier) em totalRecipeCost/sourdoughCost (issue 006) | Elimina drift IEEE-754 acumulado em ordem dos termos · golden §12: 8+0,06+0,8 → 8,86 exato (não 8,860000000000001) · algoritmo puro sem lib · NÃO é arredondamento decimal §9 (exibição-apenas), é precisão total dupla · implementado no core (regra de ouro #1: custo é do domínio) |
| 2026-07-05 | totalProductionCost = unitCost × quantity (issue 007 — resolução da inconsistência §3.E vs §12) | §3.E linha 232 redação diz "CustoTotalProdução = CustoTotalReceita × Qtd" (quebraria golden); golden §12 é contrato permanente (8,86 não 17,72) · totalProductionCost deve ser custo unitário vezes unidades produzidas, coerente com §14.3 BakeEntry.totalCost · interpretação: §3.E confundiu CustoTotalProdução com CustoTotalReceita · golden fonte da verdade · flagged para revisor humano |
| 2026-07-05 | Denominador de peso→% INCLUI fermento (issue 008) | Modo peso→%: `% = peso / (Σ pesos ingredientes + W_ferm) × 100` (§1.3 "total geral da massa" + §3.D). Fixture do teste: 800+200+700+20+200 = 1920. Só com W_ferm no denominador as % somam 100. |
| 2026-07-05 | types.ts amplia campos derivados para `number \| null` (issue 008) | HydrationSummary/RecipeSummary agora `nominalHydration?: number \| null` (nil ÷0 defensivo). Preserva contrato null≠0 (issue 005/006). Consumidores (UI 016) decidem exibição/alerta. |
| 2026-07-05 | Pricing.priceInputMode campo novo em tipos (issue 008) | Nova chave `'sale-price' \| 'margin' \| 'profit'` em Pricing, persistido por storage 011/012 e gerido por UI 016. Issue 007 implementou 3 modos isolados; tipos refletem realidade. |
| 2026-07-05 | batchPlanningMode 'total' bloqueado em issue 008 (issue 009 extensão) | recalculate assume `batchPlanningMode='total'` (toda fornada). Per-unit requer UI multi-unidade issue 009. Engine puro respeitou premissa. |
| 2026-07-05 | Per-unit em modo %→peso: F_total derivado = flourPerUnit × N (issue 009) | §2.E.1 ("planejamento por unidade apenas em %→peso"). Em recalculate, se modo %→peso E batchPlanningMode='per-unit': F_total = flourPerUnit × effectiveQuantity(quantity). effectiveQuantity reutilizado de pricing.ts garante N ≥ 1. Peso→% força 'total' (planejamento em lote inteiro). |
| 2026-07-05 | Escalonamento explícito por peso alvo (issue 009) | scaling.applyTargetScaling é ação distinta (§1.6) que clona Recipe e retorna null se alvo/soma inválidos (§5.C). Passo 3 (aplicar novos pesos) não é duplicado — delegado a recalculate via bakers.weightFromPercentage. UI deve checar null antes de re-alimentar recalculate. |
| 2026-07-05 | Agregados ponderados (issue 013) | wastageRate/averageProfitMargin do período = taxa/margem global (totalWastage/totalProduced × 100, totalProfit/totalRevenue × 100), não média aritmética das % por fornada. Motivo: sincronização com snapshots BakeEntry (unitCost/unitSalePrice §14.3); somar período requer ponderar por volume. Revisor humano: confirmar alinhamento à intenção "média" §14.4. |
| 2026-07-05 | Sem date-fns em bakes.ts (issue 013) | Operações ~8 linhas em Date nativo (getDay() offset, new Date(y,m,d) bordas). Regra de ouro 1 (libs consolidadas para não-trivial): aritmética de agrupamento é trivial. mondayOf 5 linhas, groupBy 15 linhas total. MDN consultada. |
| 2026-07-05 | StorageLike interface + createMemoryStorage (issue 011) | Storage em memória (Map) para testes Vitest em `node` sem jsdom. Regra de ouro #1/2/4 (deps consolidadas, reuso, zero novos): localStorage acessado uma única vez (local.ts); recipes.ts/prefs.ts injetam StorageLike; testes rodam em node sem nova devDependency. crypto.randomUUID fallback injetável para ambiente file:// (issue 016 UI). |
| 2026-07-05 | Storage persiste estado "cru" sem recalcular derivados (issue 011) | recipes.ts serializa Recipe[] COMPLETA (ingredientes, sourdough, modos, pricing) via JSON nativo; reviver dirigido SÓ reativa createdAt/updatedAt. Recalculate (§1.6) é responsabilidade core. Uma receita carregada é entregue sem validação/recálculo — UI (issue 016) ou smoke (issue 020) decidem. Prefs persiste showCosts booleano puro. |
| 2026-07-05 | jsdom devDep só para ingredientsTable.test.ts (issue 014) | Novo pacote jsdom adicionado. Vitest environment 'jsdom' aplicado FILE-LEVEL via `// @vitest-environment jsdom` no topo de ingredientsTable.test.ts (vite.config.ts mantém 'node' default). Justificativa (regra 1/2): 4 testes UI críticos (XSS + recálculo vivo + toggle + revert) são baratos de cobrir com jsdom; antes manual (sem regression); agora automático sem bloquear suite core (rápida em node). Regra 4 documentação: Vitest testing reference. |
| 2026-07-05 | Derivados SEM box, brandbook §4.1 > mockup (issue 014) | Peso/Custo/g/Custo na Receita (derivados) renderizam via `<td class="readonly">valor</td>` `textContent`, nunca `<input readonly>`. Decisão 24/brandbook §4.1: derivado=texto, editável=box. Mockup usa input readonly; brandbook é fonte técnica de componente, mockup é referência visual — decidimos componente. Resultado: valores finais legíveis sem interatividade falsa. |
| 2026-07-05 | .page sem max-width, cliente decide token (issue 014) | design-system.css `.page` adicionado com padding/margin apenas (tokens). Mockup tinha 1200px hardcoded; `:root` não exporta `--page-max-width`. Cliente deve decidir: criar novo token ou deixar fluido (100% viewport − margens). Bloqueador: zero (tabela funciona ambos). |
| 2026-07-05 | Modo peso→% não renderizado issue 014 (issue 016 toggle) | Plan técnico menciona `transitionToPercentageMode`, mas tabela UI não oferece botão usuário. Renderização "exibir em %" responsabilidade issue 016. Issue 014 renderiza hardcoded %→peso (`calculationMode: 'percentage-to-weight'` seed). |
| 2026-07-05 | Nome ingrediente editável spec §4 vs mockup estático (issue 014) | Spec §4 diz nome editável inline. Mockup desenha estático. Implementado conforme spec: `<input>` para editar nome com escape XSS via dom.ts. Se revisor design quiser readonly, issue 022 toca. |
| 2026-07-05 | Herança de custo §4 via hook `normalize` em state.ts (issue 015) | `AppStateStore.update` agora recebe terceiro parâmetro `normalize?: (draft: Recipe) => void` — hook síncrono chamado DEPOIS da mutação do chamador e ANTES de `recalculate`, ponto único para `inheritSourdoughFlourCosts` (sourdoughTable.ts). Sem re-entrância (roda dentro do próprio update). Extensão aditiva (014 continua 2-args compatível). Motivo: herança de custo é transformação cross-cutting (farinha-fermento ↔ farinha-principal) que não cabe em recalc (core); state é orquestrador certo. |
| 2026-07-05 | `setShowCosts` agora `notify()` após persistir pref (issue 015) | Pref global §2.A.2 consumida por 2+ tabelas agora (ingredientsTable 014, sourdoughTable 015). `setShowCosts(bool)` persiste via PrefsStore (011) DEPOIS chama `notify()` sem recalcular — estado recipe/summary intacto, apenas sinaliza assinantes para repintar class `.show-costs`/`.hide-costs`. Motivo: reutilização pref única, sincronização visual entre componentes. |
| 2026-07-05 | cellHelpers.ts extraído de 014 sem duplicação (issue 015) | `applyValidation`, `moneyPlain`, `UNIT_OPTIONS` migrados de ingredientsTable.ts para cellHelpers.ts compartilhado. Comportamento idêntico; sourdoughTable (015) reusa sem reimplementação. Regra de ouro #2 (zero duplicação). |
| 2026-07-05 | `editedCostIds` Set em memória, não persistido (issue 015) | Flag "este custo foi editado manualmente" vive em `AppStateStore.editedCostIds` (Set<string> de IDs); garante herança de custo §4 não sobrescrever manual em sessão. Limite: dados se perdem ao recarregar (F5). Persistir exigiria campo novo em types.ts Recipe/Ingredient — decisão design de dados bloqueador. Débito registrado (issue 024+). |
| 2026-07-05 | `replaceUrl` injetável em InitCalculadoraDeps (issue 040) | Campo novo `replaceUrl?: (url: string) => void` permite substituir default `history.replaceState` em testes jsdom. Motivo: lazy-create na efêmera chama `replaceUrl(...?recipe=<id>)` após criar; jsdom não toca `history` real durante testes (vi.fn mock). Default = `(url) => history.replaceState(null, '', url)` (linha 120 calculadora.ts). Padrão regra de ouro 4 (deps injetáveis garantem testabilidade). |
| 2026-07-05 | Resumo §2.B.5 generalizado para N farinhas (issue 015) | sourdoughTable renderiza Farinha 1 / Farinha 2 / … conforme `sourdough.flourIds` (regra spec: "sublinhas por farinha do fermento" §2.B.3 sem limite explícito). Mockup desenha single-flour isolada. Precedente 014: spec > mockup para lógica (brandbook > mockup para UI estática). Issue 022 (follow-up design) pode estilizar para mockup-único se cliente priorizar simplificar visual. |
| 2026-07-05 | Formatação formatWeight nunca agrupa milhar, fixa 1100,0 não 1.100,0 (issue 015) | Spec §9 exige `useGrouping:false` (gabarito 1041,7 não 1.041,7). formatWeight é dono único (format.ts) — hydrationPanel usa para "Farinha Real Consumida". Mockup prosa/mockup.html usa grafia solta "1.100,0"; code-of-truth é formatWeight (regra 2: dono único de formato). Precedente 014: spec §9 > mockup visual. |
| 2026-07-05 | Toggle de modo com banner sticky e classe body.mode-alt (issue 016) | modeToggle.ts renderiza botão "Alternar p/ peso → %" + gerencia banner obrigatório (§1.3 "sinalização visual obrigatória"). Banner inserido/removido de `document.body` conforme `calculationMode`; classe `body.mode-alt` ativa seletor design-system `.mode-alt .cell-input.pct` para destaque de % em modo peso→%. Self-contained: gerencia própria reatividade via `store.subscribe`, montado uma vez em batchPanel (regra 014: nunca recriar input focado) + no banner global. Motivo: centraliza sinalização visual do modo; evita duplicação assinatura. |
| 2026-07-05 | % com classe .pct em modo peso→% renderizada como input readonly (issue 016) | Decisão 2 (PROGRESS): em modo peso→%, % é campo readonly (usuário vê valor atualizado, não edita). Implementação: `<input readonly class="pct">` (não `<span>`, mantém mesma célula; classe `.pct` ativa seletor `.mode-alt .cell-input.pct` design-system). Leve exceção a decisão 24 (derivado=texto), documentada em cabeçalho ingredientsTable.ts. Motivo: % precisa ser VISÍVEL inline (mesma linha do peso), input readonly é component mais natural para "apenas leitura interativa" no design system. |
| 2026-07-05 | applyTransform em state.ts para ações explícitas (issue 016) | Novo método `AppStateStore.applyTransform(fn: (recipe) => Recipe \| null)` (síncrono): executa transformação core (ex: `transitionToPercentageMode`, `applyTargetScaling`), recalcula se retorna Recipe, **não notifica se retorna null** — silenciosamente bloqueia ação inválida (modo errado, alvo/soma inválidos §5.C). Motivo: ações explícitas (escalonamento, transição de modo) já validadas no core; UI não precisa duplicar — botão desabilitado é reforço visual, mas lógica é no core. Padrão clean de composição: modeToggle/scalePanel delegam validação ao core, não fazem check local. |
| 2026-07-05 | modeToggle e scalePanel montados uma única vez em batchPanel (issue 016) | `renderBatchPanel` chama `renderModeToggle(subelement)` e `renderScalePanel(subelement)` uma única vez durante montagem, injeta ambos em filhos fixos. Não são remontados a cada mudança estrutural — apenas seus dados repintados via `subscribe`. Motivo: regra 014 (nunca recriar input focado); evita assinatura duplicada; padrão clean (batchPanel é card, modeToggle/scalePanel são sub-widgets autorresponsáveis). |
| 2026-07-05 | pricingPanel sincroniza trio preço/margem/lucro sem sobrescrever campo focado (issue 016) | Campo em edição NUNCA é sobrescrito por `subscribe` — `activeField` marca qual dos três está sendo digitado (Preço/Margem %/Lucro). Ao receber notificação de `store.subscribe`, repinta apenas demais dois. Blur formata (§9) e limpa `activeField`, liberando para repintura normal. Motivo: UX — usuário pode ver o valor atualizar enquanto digita sem se sentir "brigando" com a calculadora. Padrão Orval/React (evita re-renderização indesejada de input focado). |
| 2026-07-05 | Ingredientes editáveis por modo: % vs peso conforme calculationMode (issue 016) | ingredientsTable.ts extensão: `calculationMode === 'weight-to-percentage'` controla quais campos são editáveis. Modo %→peso: % editável, peso readonly. Modo peso→%: peso editável, % readonly com classe `.pct`. Renderização: `readOnly`/`disabled` atributos nativos, nunca falsifica input (decisão 24 reuse total). Cabeçalho atualizado: §1.2/§1.3. |
| 2026-07-05 | .row / .row--end promovidas ao design-system.css (issue 016) | Revisão guardiao-design: `.field-row` duplicava `.row` em batchPanel/pricingPanel (violava regra 2: zero duplicação). Solução: classes `.row` e `.row--end` (flexbox layout, espaçamento entre items) movidas para design-system.css como utilidades reutilizáveis. `.field-row` removida. Todas as instâncias em batchPanel, pricingPanel, scalePanel atualizadas. Extensão design-system.css: novo seletor `.row { display: flex; ... }` com variante `.row--end { justify-content: flex-end; }`. Documentado em design-system.html. |
| 2026-07-05 | Classes .mode-alt .cell-input.pct e .chip-ok/.chip-warn/.chip-crit no design-system (issue 016) | design-system.css estendido (issue 016): seletor `.mode-alt .cell-input.pct` (aplica destaque quando body tem classe `mode-alt`; ativa cor de fundo/borda para % readonly em modo peso→%); `.chip-ok` / `.chip-warn` / `.chip-crit` (variantes de chip pricingPanel, cores por marginStatus); `.loss` classe para destaque prejuízo. Tokens usados: cores do brand, espaçamento. Documentado em design-system.html. |
| 2026-07-05 | marginChipClass extraído para cellHelpers (issue 017) | pricingPanel.ts dualizava mapa `MarginStatus → .chip-*` localmente como `CHIP_CLASS`. recipesList.ts (issue 017) precisa do mesmo mapa. Solução: `marginChipClass(status)` função novo em cellHelpers.ts (ponto único, regra 2). pricingPanel agora importa de cellHelpers. Motivo: zero duplicação, fonte de verdade única para chip de margem em múltiplos componentes. |
| 2026-07-05 | Auto-save em calculadora.ts com debounce + flush (issue 017) | `pages/calculadora.ts` suporta `?recipe=<id>`: carrega via `recipeStore.get`, fallback `goldenSeed()`. Cada `store.update` (input/blur) persiste auto via `setTimeout` debounce 400ms (reprime múltiplas edições). Flush garantido: `visibilitychange` (abate aba) e `beforeunload` (fecha browser). async via `Promise.resolve()` sem bloquear navegação. Consequência: localStorage recebe Recipe com derivados preenchidos (custo/margem). Revisor: confirmar aceitável vs "storage persiste apenas estado editável" (decisão 011.3). Debate registrado como decisão 017.1 em PROGRESS. |
| 2026-07-05 | Backup validação ANTES escrita + status region aria-live (issue 017) | recipesList importa backup (012) + bakes (013 seam). Exportar: blob JSON aaaa-mm-dd*.json (objectURL native API). Restaurar: `readBackupFile` promessa → `importBackup` 012 valida envelope ANTES escrita → `applyBackupData` 012 substitui total (receitas 011 + bakes 013 via BAKES_STORAGE_KEY) SE valida, SEM cascade (§14.7). Erro: status region `role="status" aria-live="polite"` mostra pt-BR sem crash; dados anteriores intactos. Padrão: nenhum try/catch silencioso, UI sempre responsável por mostrar erro. |
| 2026-07-05 | Confirm/prompt nativos injetáveis em recipesList (issue 017) | Excluir: `window.confirm(msg avisos órfãs)`, renomear: `window.prompt(nome)`, restaurar erro: `window.alert`. Padrão nativo bloqueante (menos UX amigável que modal, MAS design-system não tem modal). Função recebe `deps` objeto: `{ confirm?, prompt?, navigate?, alert?, ... }` — testes injetam `vi.fn()` (zero `window.*` real em jsdom). Produção usa native. Motivo: zero nova dependência modal (regra 1), determinismo teste. |
| 2026-07-05 | Abrir receita navega index.html?recipe=<id>, não rota (issue 017) | Link no card: `<a href="/index.html?recipe=${id}">Abrir</a>` (URL param simples, parsing nativo). Offline-safe (app MPA Vite puro, não SPA Router). Calculadora.ts lê `URLSearchParams(location.search).get('recipe')` → carrega ou fallback. Sem biblioteca router/history, sem estado global (regra 1: libs consolidadas, zero extras para single param). |
| 2026-07-05 | Gráfico em SVG puro, sem Chart.js/uPlot (issue 018) | trendChart.ts renderiza plotagem pontos+polyline em `<svg>` via createElement namespace + helper svg() (dom.ts). Chart.js ~254kB minificado, uPlot ~30kB — ambas reconstruiriam visual que mockup + design-system já entregam via classes CSS. Motivo: mockup é SVG estaticamente, design-system.css exporta todas as classes de estilo; custo de lib vs reuso visual existente desfavorável. Regra de ouro 1: libs para não-trivial; plotagem neste escopo é operação direta (trivial). Sem animação v1 (polish, issue 026+). |
| 2026-07-05 | parseLocalDate dono único de parse de data local, nunca new Date('aaaa-mm-dd') (issue 018) | Novo `parseLocalDate(dateString)` em format.ts cria Date meia-noite local via `new Date(year, monthIndex, day)` nativo. Bloqueia bug 021: `new Date('aaaa-mm-dd')` internamente cria meia-noite UTC e exibe deslocada ao usuário brasileiro. Usado ubiquamente por bakeForm (input date) e historyView (filtro períodos) como centralizado parser. TDD: round-trip 'aaaa-mm-dd' ↔ Date ↔ formatDate idempotente, getTime() offset local não UTC. |
| 2026-07-05 | ExcelJS 4.4.0 como dependency (SheetJS rejeitada por CVEs, issue 019) | Candidatas avaliadas: SheetJS 0.18.5 (congelada, CVE-2023-30533 High + CVE-2024-22363 sem fix) REJEITADA. exceljs 4.4.0 (dependency produção, 0 CVEs, writeBuffer() offline §10, MDN consultada regra 4). Primeira lib de runtime. Bundle warn 500kB raw/273kB gzip → issue 027 follow-up (code-split dinâmico). Célula XLSX = número + numFmt (recalculável, arredonda exibição moneyPlain §9). Motivo: segurança CVE (regra 3), reuso visual §8, offline 100% cliente. |
| 2026-07-05 | KPIs historyView pré-filtram !isPlanned antes groupBy (issue 018) | historyView renderiza tabela TODAS as fornadas (confirmadas + planejadas com badge "planejada"), MAS KPIs (Desperdício %, Margem %, Produzido kg, Vendido kg) e gráfico PRÉ-filtram `!isPlanned` antes de aggregatePeriod/groupBy — motiva decisão 018.3. Gráfico só plota confirmadas (eixo X dias sem pontos futuros). Planejadas adicionam badges na tabela, não afetam métricas/comparação. Mitiga bug 021: UI nunca calcula agregados com dados futuros. |
| 2026-07-05 | Edição inline histórico limitada a produzida/vendida (issue 018) | historyView tabela renderiza colunas Data/Receita/Custo readonly (snapshot imutáveis §14.2), Produzidas/Vendidas editáveis inline. Motivo: receita e data são do registro original (não se retroativamente revisam), custo unitário é snapshot da época (imutável). Se cliente quer ajuste custo/preço/receita, cria nova fornada ou feature 019+. Simplificação: excluir (trash) vs editar receita previa (recriar). |
| 2026-07-05 | BakeEntry snapshot gravado em 2 casas via moneyPlain, novo estado puro (issue 018) | Quando usuário confirma planejada em bakeForm, `totalCost`/`unitCost`/`unitSalePrice` são snapshots da época calculados via `computeBakeDerived` (013), formatados 2 casas via `moneyPlain` formatter (moeda sem símbolo R$). Exemplo: `cost: 8.86, unitCost: 4.43, unitSalePrice: 7.38` armazenam número puro. BakeEntry é novo estado (§14.2 "fatos da fornada em um instante"), não referência Recipe — snapshot nunca retroativamente muda se receita muda. Não viola decisão 011 ("storage persiste estado cru") porque BakeEntry é entidade própria, não derivada. |
| 2026-07-05 | Inversão nome↔conteúdo `index.html` ↔ `receitas.html` — rota inicial muda para Receitas (issue 032) | GitHub Pages serve `index.html` como default `/minhapadaria/` — nenhuma forma de contornar sem trocar conteúdo HTML. Decisão: **swap literal** — `index.html` agora hospeda shell de Receitas (antes Calculadora), `receitas.html` agora hospeda shell de Calculadora (antes Receitas); nomes preservados, conteúdo trocado. vite.config.ts rollupOptions.input inalterado. `historico.html` nav links atualizados (Calculadora → receitas.html, Receitas → index.html). `recipesList.ts`: 3 hrefs `index.html?recipe=` → `receitas.html?recipe=` (criar-com-seed, criar-em-branco, card Abrir). **Consequência**: URLs antigas (`/index.html?recipe=<id>`) quebram → novo destino `/receitas.html?recipe=<id>`. Registrada como decisão consciente (cliente aceitou, nenhuma URL publicada em produção). Comentários HTML explícitos em index.html e receitas.html explicam a inversão (mitiga confusão). Rastreabilidade: issue 032 decisões 032.1–032.2 em PROGRESS. |
| 2026-07-05 | 1º modal do design system — exceção explícita ao registro "sem modal" (issue 035) | Decisão anterior (linha "Confirm/prompt nativos injetáveis em recipesList", issue 017) registrava "design-system não tem modal" como motivo para usar `window.confirm`/`window.prompt`. Pedido explícito do cliente na issue 035 muda isso SÓ para o fluxo "+ Nova receita": `openPromptModal` (`src/ui/modal.ts`) — overlay+caixa (`div role="dialog" aria-modal="true" aria-labelledby"`, NÃO `<dialog>` nativo: `jsdom` do projeto não implementa `showModal()`/`close()`, o que impediria testar Esc/backdrop/foco-preso em jsdom — decisão registrada no Plano Técnico da issue 035). Novas classes `.modal-overlay`/`.modal` (só tokens, token novo `--overlay` ~50% de `--tinta`) em design-system.css/.html. Renomear (issue 033) e excluir/restaurar backup (issue 017) CONTINUAM nativos — a exceção é restrita à criação de receita, não uma reversão geral da decisão 017. `goldenSeed()` deixa de incluir o ingrediente Azeite (categoria `fat`) e o nome padrão muda de "Pão Rústico de Azeite" para "Pão Rústico". |
| 2026-07-06 | Utilitária `.stack` para empilhamento vertical (issue 042) | Novo seletor `.stack` em design-system.css (L502–510): flexbox column + `gap: var(--sp-3)` para empilhar campos verticalmente. Utilizado por pricingPanel (issue 042): 4 campos Custo unitário → Lucro unitário → % de lucro → Preço de venda em coluna (antes estavam lado-a-lado `.row.row--end`). Só tokens, sem valor bruto novo. Reusável para futuros casos de empilhamento. Documentada em design-system.html. Motivo: evitar duplicação de styles inline ou flexbox + gap escopadas a um componente — reutilização regra 2. |
