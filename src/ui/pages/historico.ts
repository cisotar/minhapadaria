/**
 * historico.ts — Entry da página Histórico de Fornadas (historico.html) · Spec §14.
 *
 * O que faz: por ora apenas carrega os tokens do design system.
 * Implementa registro e análise de fornadas: data, ingredientes usados,
 * custo total, notas do padeiro, gráficos de tendência (§14).
 *
 * A UI completa (espelhando mockups/historico.html) chega na issue 018.
 */
// Fonte única de tokens (architecture.md §Estilo). Nunca duplicar/editar tokens.
import '../../../references/design-system.css';
